# Results

This directory is empty and will be populated when you run the training scripts.

After running `python scripts/reproduce_paper_results.py`, you will find:

- `paper_results.json` — results table for all models
- `gnn/` — TraceGNN training logs, checkpoints, and per-epoch metrics
- `validation/` — per-trace validation results (if you re-run the validation pipeline)
